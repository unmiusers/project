import React from 'react';
import VersionControl from '../components/VersionControl/VersionPage';
import './VersionPage.css';

const VersionPage = () => {
    return (
        <div className="version-page">
            <VersionControl />
        </div>
    );
};

export default VersionPage;